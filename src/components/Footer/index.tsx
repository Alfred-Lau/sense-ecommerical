import { FC } from "react";
import styles from "./index.module.scss";

const Footer: FC = (props) => {
  return (
    <div className={styles.footer}>
      <div className={styles.container}>天工体系垂直场景 - 电商</div>
    </div>
  );
};

export default Footer;
